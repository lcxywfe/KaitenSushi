import os
import shutil
from glob import glob
from setuptools import setup, find_packages

setup(
    name="kaitensushi",
    version="0.0.1",
    author="hy",
    author_email="lcxywfe@gmail.com",
    description="",
    packages=find_packages(),
    python_requires='>=3.8',
)
